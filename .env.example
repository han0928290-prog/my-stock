# 複製成 .env.local 並填入自己的值（.env.local 不會被 commit）

# MongoDB 連線字串
MONGODB_URI=

# FinMind API token（選填，有填額度較高）
FINMIND_TOKEN=

# 簽發登入 JWT 用的密鑰，請用隨機長字串，例如：node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=

# 註冊時要輸入的驗證碼；沒設定時所有註冊都會被拒絕
REGISTER_CODE=
